import os;

struct Point {
    int x;
    int y;
};

int main() {
    os.println("Array Literal Test");
    
    var arr = [1, 2, 3];
    os.println("Array length: " + os.str(arr.length));
    os.println("arr[0] = " + os.str(arr[0]));
    
    var arr2 = ["hello", "world"];
    os.println("arr2[1] = " + arr2[1]);
    
    return 0;
}
